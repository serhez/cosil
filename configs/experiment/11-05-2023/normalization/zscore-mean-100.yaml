# @package _global_
---
defaults:
  - base_experiment

method:
  # CoSIL
  normalization_type: z_score
  normalization_mode: mean
  normalization_gamma: 100.0

logger:
  group_name: "11-05-2023_normalization"
  experiment_name: "zscore-mean-100"
