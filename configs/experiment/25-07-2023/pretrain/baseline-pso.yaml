# @package _global_
---
defaults:
  - base_pretrain
  - override /method: cosil2
  - override /method/agent: dual_sac
  - override /method/rewarder: airl  # variable

#### Variables
learn_disc_transitions: false

method:
  replay_buffer_path: /scratch/work/hernans2/cosil/replay_buffers/GaitTrackHalfCheetah-v0/seed-123456/dualsac-gail-pretrain_1689060342.pt
  pretrain: false
  pretrain_path: null
  dual_mode: q
  omega_scheduler: constant
  num_episodes: 50
  episodes_per_morpho: 50
  morpho_warmup: 0
  start_steps: 0
  disc_warmup: 0
  eval: false
  record_test: false
  save_checkpoints: false
  save_optimal: false
  save_final: true
  co_adaptation:
    morphos_path: null
    dist_optimizer: pso

  #### Variables

  # General
  transfer: false
  omega_init: 0.0
  adapt_morpho_omega: 0.0
  demos_strategy: replace

  # Rewards normalization
  rewarder:
    log_scale_rewards: false
    norm_type: none
    norm_mode: mean
    rl_norm_gamma: 1.0
    rl_norm_beta: 0.0
    il_norm_gamma: 1.0
    il_norm_beta: 0.0
    norm_low_clip: null
    norm_high_clip: null

  # Agent normalization and entropy
  agent:
    alpha: 0.05
    automatic_entropy_tuning: false
    norm_type: none
    norm_mode: mean
    rl_norm_gamma: 1.0
    rl_norm_beta: 0.0
    il_norm_gamma: 1.0
    il_norm_beta: 0.0
    norm_low_clip: null
    norm_high_clip: null


logger:
  group_name: "pretrain-baseline-pso"
  experiment_name: "baseline-pso"
  loggers: console,wandb
