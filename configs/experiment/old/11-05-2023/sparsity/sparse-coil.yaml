# @package _global_
# TODO: Use best-performing config of prev. experiments in base_halfcheetah_experiment
---
defaults:
  - base_halfcheetah_experiment
  - override /method: coil
  - override /method/agent: sac
  - override /method/rewarder: gail

method:
  sparse_mask: 5.5

logger:
  group_name: "11-05-2023_sparsity"
  experiment_name: "sparse-coil"
