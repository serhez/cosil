# @package _global_
# TODO: Use best-performing config of prev. experiments in base_halfcheetah_experiment
---
defaults:
  - base_halfcheetah_experiment
  - override /method: cosil
  - override /method/agent: dual_sac
  - override /method/rewarder: gail  # not used, as omega is permanently 0.0

method:
  omega_scheduler: constant
  omega_init: 0.0
  normalization_type: none

logger:
  group_name: "11-05-2023_methods"
  experiment_name: "cosil-rl"
