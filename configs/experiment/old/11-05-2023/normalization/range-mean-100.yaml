# @package _global_
---
defaults:
  - base_halfcheetah_experiment

method:
  # CoSIL
  normalization_type: range
  normalization_mode: mean
  normalization_gamma: 100.0

logger:
  group_name: "11-05-2023_normalization"
  experiment_name: "range-mean-100"
